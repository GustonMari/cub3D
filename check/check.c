#include "../function.h"

int check_line_edge(char *str, int nb_line)
{
    int i;
    int first;

    i = 0;
    while (str[i])
    {
        if 
        i++;
    }
    return (TRUE)
}

int check_map(char **map, int nb_line)
{
    int i;
    int j;

    i = 0;
    while (map[i])
    {
        j = 0;
        while (map[i][j])
        {

            j++;
        }
        i++;
    }
}

int ft_check(char **map, int nb_line)
{
    if (check_map == FALSE)
    {
        ft_putstr_error("Wrong map\n");
        exit(0);
    }
}